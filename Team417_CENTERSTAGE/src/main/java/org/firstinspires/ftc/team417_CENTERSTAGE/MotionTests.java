package org.firstinspires.ftc.team417_CENTERSTAGE;
/*
import com.acmerobotics.roadrunner.Pose2d;
import com.acmerobotics.roadrunner.TrajectoryActionBuilder;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

import org.firstinspires.ftc.team417_CENTERSTAGE.baseprograms.BaseOpMode;
import org.firstinspires.ftc.team417_CENTERSTAGE.baseprograms.PropDistanceFactory;
import org.firstinspires.ftc.team417_CENTERSTAGE.roadrunner.MecanumDrive;

@TeleOp(name = "Motion Tests")
public class MotionTests extends BaseOpMode {

    public void runOpMode() {
        driveFourFeet();
    }

    private void setPower(double power) {
        drive.leftFront.setPower(power);
        drive.rightFront.setPower(power);
        drive.leftBack.setPower(power);
        drive.rightBack.setPower(power);
    }

    public void driveFourFeet() {
        Pose2d startPose = new Pose2d(0, 0, 0);

        TrajectoryActionBuilder builder
                = this.drive.actionBuilder(startPose)
                .splineToLinearHeading(new Pose2d(96, 0, 0), 0);
        return new PropDistanceFactory.PoseAndAction(builder.build(), startPose);
    }
}*/
